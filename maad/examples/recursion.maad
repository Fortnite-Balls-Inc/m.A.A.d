routine Factorial(n : integer) : integer is
    if n <= 1 then
        return 1    
    else
        return n * Factorial(n - 1)    
    end
end

routine Main(n : integer) is
    print Factorial(n)
end